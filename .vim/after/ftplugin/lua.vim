setlocal shiftwidth=4                 " Sets indent length (<<, >>, autoindent).
setlocal softtabstop=4                " How many spaces a <Tab> takes up (edit mode)

setlocal tags+=~/.vim/tags/Lua.tag
